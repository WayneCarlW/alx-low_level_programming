#include <stdio.h>
#include "main.h"
/**
 * _isdigit - checks if input is a digit
 * @c: tested character
 * Return: returns 1 if it is uppercase, 0 if not
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
