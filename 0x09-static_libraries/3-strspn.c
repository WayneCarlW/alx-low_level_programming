#include <stdio.h>
/**
 * _strspn - calculates the length of the initial segment
 * s which entirely consists of bytes in accept
 *
 * @s: pointer to contiguous memory block containing a string
 * @accept: Test string
 * Return: the number of bytes of the initial segment
 * that entirely consists of bytes from accept
 */
unsigned int _strspn(char *s, char *accept)
{
}
