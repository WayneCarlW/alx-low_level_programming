#include <stdio.h>
/**
 * _strpbrk - locates the first occurrence of string s of any of the
 * bytes in the string accept
 * @s: substring
 * @accept: string
 * Return: the pointer to the byte s that matches one
 * of the bytes in accept or NULL otherwise
 */
char *_strpbrk(char *s, char *accept)
{
}
