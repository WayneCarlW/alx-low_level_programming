#include <stdio.h>
/**
 * _strcpy - Copies the string of a variable to the other
 *
 * @dest: Destination
 * @src: Source
 * Return: pointer to the destination
 */
char *_strcpy(char *dest, char *src)
{
	char *ordest = dest;

	while ((*dest++ = *src++) != '\0')
	{
	}
	return (ordest);
}
