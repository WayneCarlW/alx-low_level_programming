#include "main.h"
/**
 * main - returns the name of the program
 * @argc: number of commandline arguments
 * @argv: Array of args of size argc
 * Return: 0 Success
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
